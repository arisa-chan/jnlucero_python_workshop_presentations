---
format:
  live-revealjs:
    scrollable: true
theme: solarized
engine: knitr
pyodide:
  packages:
    - matplotlib
    - numpy
    - pandas
    - openpyxl
filters:
  - highlight-text
---

<!-- SLIDE: Title Page -->

::: {layout-ncol=1 style="width: 21%; margin: auto;"}
<img src="https://tse3.mm.bing.net/th/id/OIP.f2gsyFeBRC3wjhSH_H5KRgHaHa?rs=1&pid=ImgDetMain&o=7&rm=3" width="96" height="96"/> <img src="https://logos-download.com/wp-content/uploads/2016/10/Python_logo_icon.png" width="96" depth="96"/>
:::

<h1 align="center">From Syntax to Solution: Basic Application of Python to Data Science</h1>
<h3 align="center">DLSU Data Science Society Python Workshop</h3>

::: {style="text-align: center; font-size: 16pt"}
**Jaydee N. Lucero**<br/>Senior Structural Engineer I<br/>Abinales Associates Engineers + Consultants
:::

<!-- SLIDE: Table of Contents -->

## Contents
::: {style="font-size: 20pt"}

_**Part One:** Some Python stuff<br/>_
1 --- Writing code Pythonically: list comprehensions<br/>
2 --- Writing faster code: lists vs. sets vs. dictionaries<br/>
3 --- Writing functions that take in functions<br/>
4 --- Writing code using the "Big 3" libraries<br/>
    - `numpy`<br/>
    - `pandas`<br/>
    - `matplotlib`
<br/><br/>
_**Part Two:** Some Python examples<br/>_
5 --- Data science example using Python<br/>
:::

<!-- SLIDE: Topic 2 -->

# 1 <br/>Write code Pythonically

<!-- SLIDE: Topic 2 Example -->

## Writing code Pythonically

::: {.fragment .fade-in style="font-size: 22pt"}
[**Example 1**]{color="white" bg-color="black"} Find the sum of the first 100 positive perfect square numbers.
:::
::: {.fragment .fade-in style="font-size: 22pt"}
[**Manual computation**]{color="white" bg-color="gray"}
$$\sum_{n=1}^{100} n^2 = \left.\frac{n(n+1)(2n+1)}{6}\right\lvert_{n=100}=\frac{100(100+1)(2\cdot 100 + 1)}{6} = 338350$$
:::

::: {.fragment .fade-in style="font-size: 22pt"}
[**Solution 1**]{color="white" bg-color="gray"} using `for` loops
```{pyodide-python}
total = 0
for n in range(1, 100 + 1) :
  total += n**2
print(total)
```
:::

<!-- SLIDE -->

## Writing code Pythonically

::: {style="font-size: 22pt"}
[**Example 1**]{color="white" bg-color="black"} Find the sum of the first 100 positive perfect square numbers.
:::

::: {.fragment .fade-in style="font-size: 22pt"}
[**Solution 2**]{color="white" bg-color="gray"}
```{pyodide-python}
print(sum([n**2 for n in range(1, 100 + 1)]))
```
:::

::: {.fragment .fade-in style="font-size: 22pt"}
::: {bg="#c9f5d2"}
[**List comprehensions**]{color="white" bg-color="#1c7a2f"}

`[(1) for (2) in (3) if (4)]`<br/>generates a list of items generated by expression (1) for each item (2) in the iterable (3) based on conditions set by (4).
:::
:::

## Writing code Pythonically

::: {style="font-size: 22pt"}
[**Example 2**]{color="white" bg-color="black"} Suppose that we have the following record of transactions in US Dollars. Notice that some transactions are corrupted (the amount is either `None` or negative value). We want to determine the total amount of all valid transactions only, converted to Euros using the conversion 1 US Dollar = 0.85 Euro.
:::

::: {.fragment .fade-in style="font-size: 22pt"}
[**Solution**]{color="white" bg-color="gray"}
```{pyodide-python}
transactions_usd = [10.50, None, -5.00, 25.00, 100.00, None, 12.50]
usd_to_euro = 0.85

total_euros = sum([amount * usd_to_euro for amount in transactions_usd if amount is not None and amount > 0])
print(total_euros)
```
:::

<!-- SLIDE: Topic 2 -->

# 2 <br/>Write faster code

<!-- SLIDE: Topic 2 -->

## Writing faster code

::: {.fragment .fade-in style="font-size: 22pt"}
::: {bg="#c9f5d2"}
[**Some ways to make code faster**]{color="white" bg-color="#1c7a2f"}

::: {.incremental}
- Use `numpy` arrays instead of lists.
- Use raw iterator objects instead of lists when using all its contents.
- Use dictionaries instead of lists.
- Use sets instead of lists.

::: {.fragment}
*Containment time complexities*

| Data structure | Average | Worst case    |
|:--------------:|:-------:|:-------------:|
| List/Tuple     | O(n)    | O(n)          |
| Dictionary     | O(n)    | O(n)          |
| Set            | O(1)    | O(n)          |
:::

::: {.fragment}
*Iteration time complexities*

| Data structure | Average | Worst case    |
|:--------------:|:-------:|:-------------:|
| List/Tuple     | O(n)    | O(n)          |
| Dictionary     | O(n)    | O(n)          |
| Set            | O(n)    | O(n)          |
:::

:::

:::
:::

## Writing faster code

::: {.fragment .fade-in style="font-size: 22pt"}
[**Example**]{color="white" bg-color="black"} Suppose that we have the following record of transactions in US Dollars generated from one million random items. Notice that some transactions are corrupted (the amount is either `None` or negative value).

We want to know if a particular transaction value is in the list.
:::

::: {.fragment .fade-in style="font-size: 22pt"}
[**Solution**]{color="white" bg-color="gray"} Using lists/tuples.
```{pyodide-python}
import time
import numpy as np

# Generate one million random transactions and place it inside a list.
transactions_usd = []
for _ in range(1000000) :
  choice = np.random.randint(3)
  if choice == 0 :
    transactions_usd.append(None)
  elif choice == 1 :
    transactions_usd.append(np.random.randint(100000001)*-0.000001)
  elif choice == 2 :
    transactions_usd.append(np.random.randint(100000001)*0.000001)

item = 67.89        # Find this transaction.

t_i = time.time()   # Start timer

if item in transactions_usd :
  print(f"Found {item}.")
else :
  print(f"Not found.")

t_f = time.time()   # End timer

# Calculate run time.
print(f"calculation time = {t_f - t_i:.4f} seconds.")
```
:::

## Writing faster code

::: {.fragment .fade-in style="font-size: 22pt"}
[**Example**]{color="white" bg-color="black"} Suppose that we have the following record of transactions in US Dollars generated from one million random items. Notice that some transactions are corrupted (the amount is either `None` or negative value).

We want to know if a particular transaction value is in the list.
:::

::: {.fragment .fade-in style="font-size: 22pt"}
[**Solution**]{color="white" bg-color="gray"} Using dictionaries.
```{pyodide-python}
import time
import numpy as np

# Generate one million random transactions and place it inside a list.
transactions_usd = []
for _ in range(1000000) :
  choice = np.random.randint(3)
  if choice == 0 :
    transactions_usd.append(None)
  elif choice == 1 :
    transactions_usd.append(np.random.randint(100000001)*-0.000001)
  elif choice == 2 :
    transactions_usd.append(np.random.randint(100000001)*0.000001)

# Convert to a dictionary.
transactions_usd = dict(enumerate(transactions_usd))

item = 67.89        # Find this transaction.

t_i = time.time()   # Start timer

if item in transactions_usd.values() :
  print(f"Found {item}.")
else :
  print(f"Not found.")

t_f = time.time()   # End timer

# Calculate run time.
print(f"calculation time = {t_f - t_i:.4f} seconds.")
```
:::

## Writing faster code

::: {.fragment .fade-in style="font-size: 22pt"}
[**Example**]{color="white" bg-color="black"} Suppose that we have the following record of transactions in US Dollars generated from one million random items. Notice that some transactions are corrupted (the amount is either `None` or negative value).

We want to know if a particular transaction value is in the list.
:::

::: {.fragment .fade-in style="font-size: 22pt"}
[**Solution**]{color="white" bg-color="gray"} Using sets.
```{pyodide-python}
import time
import numpy as np

# Generate one million random transactions and place it inside a list.
transactions_usd = []
for _ in range(1000000) :
  choice = np.random.randint(3)
  if choice == 0 :
    transactions_usd.append(None)
  elif choice == 1 :
    transactions_usd.append(np.random.randint(100000001)*-0.000001)
  elif choice == 2 :
    transactions_usd.append(np.random.randint(100000001)*0.000001)

# Convert to a set.
transactions_usd = set(transactions_usd)

item = 67.89        # Find this transaction.

# Calculate total of all valid transactions and convert to Euros.
t_i = time.time()   # Start timer

if item in transactions_usd :
  print(f"Found {item}.")
else :
  print(f"Not found.")

t_f = time.time()   # End timer

# Calculate run time.
print(f"calculation time = {t_f - t_i:.4f} seconds.")
```
:::

<!-- SLIDE: Topic 3 -->

# 3 <br/>Write functions that take in functions

<!-- SLIDE -->

## Functions as objects

::: {.fragment .fade-in style="font-size: 22pt"}
::: {bg="#c9f5d2"}
[**Functions as objects**]{color="white" bg-color="#1c7a2f"}

Function _names_ can be inputted as arguments in a function.
:::
:::

::: {.fragment .fade-in style="font-size: 22pt"}
[**Example**]{color="white" bg-color="black"} Solve the equation $e^x = x+5$ using Newton-Raphson method.
:::

::: {.fragment .fade-in style="font-size: 22pt"}
[**Solution**]{color="white" bg-color="gray"}
```{pyodide-python}
import math

# The equation, expressed as left - right. This should be equal to zero.
eqn = lambda x : math.exp(x) - x - 5

# Define a function that takes an equation (a function) as an argument.
def newton_raphson(func, x0=0, tol=1e-6) :
    xn = x0     # guess value
    h = 1e-6    # marching step for numerical differentiation

    # Check if the equation is close to zero.
    while abs(func(xn)) >= tol :
        d_func = (func(xn + h) - func(xn - h))/(2*h)    # derivative
        xn -= func(xn)/d_func                           # new guess value

    return xn   # root found

print(newton_raphson(eqn))
```
:::

<!-- SLIDE -->

## Functions as objects: applications to pandas

::: {.fragment .fade-in style="font-size: 22pt"}
[**Example**]{color="white" bg-color="black"} UP Diliman implements the following honorific scholarships based on the general weighted average (GWA) of a student for a given semester.

| Item                    | Range                         |
|:-----------------------:|:-----------------------------:|
| University Scholar (US) | GWA $\leqslant$ 1.45          |
| College Scholar (CS)    | 1.45 $<$ GWA $\leqslant$ 1.75 |
| No scholarship          | GWA $>$ 1.75                  |

Given a `pandas` dataframe of students and their corresponding GWA for a specific semester, output the honorific scholarship of each student.
:::

::: {.fragment .fade-in style="font-size: 22pt"}
[**Solution**]{color="white" bg-color="gray"}
```{pyodide-python}
import pandas as pd

data = {
  "Students" : ["Jinx", "Lux", "Ahri", "Annie", "Ezreal"],
  "GWA"      : [1.1356, 1.2578, 1.6703, 1.8935, 2.2410]
}
data = pd.DataFrame(data)

def scholarship(gwa) :
    if gwa <= 1.45 :
        return "University Scholar (US)"
    elif gwa <= 1.75 :
        return "College Scholar (CS)"
    else :
        return "No scholarship"

data["Scholarship"] = data["GWA"].apply(scholarship)
print(data)
```
:::

# 4 <br/>Write code using the "Big 3" libraries

## The `numpy` library

::: {.fragment .fade-in style="font-size: 22pt"}
[**Example**]{color="white" bg-color="black"} The Civil Engineering Licensure Examination has three subjects with codes MSTC, HGE and PSAD. A future engineer is required to take all these three subjects. His/Her overall rating is then calculated according to the formula $$\text{rating} = 0.35(\text{MSTC}) + 0.30(\text{HGE}) + 0.35(\text{PSAD})$$. He/She is said to have passed the exam if the following conditions are satisfied:

- $\text{rating} \geqslant 70$
- $\text{MSTC, HGE, PSAD} \geqslant 50$

Given the results from ten students as shown below, determine whether each student has passed the exam.
:::

## The `numpy` library

::: {.fragment .fade-in style="font-size: 22pt"}
[**Example**]{color="white" bg-color="black"}

| Student # | MSTC | HGE | PSAD |
|:---------:|:----:|:---:|:----:|
| 1         | 96   | 88  | 90   |
| 2         | 92   | 85  | 49   |
| 3         | 89   | 84  | 83   |
| 4         | 83   | 80  | 75   |
| 5         | 72   | 83  | 86   |
| 6         | 66   | 68  | 47   |
| 7         | 59   | 52  | 56   |
| 8         | 51   | 63  | 62   |
| 9         | 42   | 30  | 96   |
| 10        | 38   | 28  | 58   |
:::

## The `numpy` library

::: {style="font-size: 22pt"}
[**Example**]{color="white" bg-color="black"}

Given the results from ten students as shown below, determine whether each student has passed the exam.
:::

::: {.fragment .fade-in style="font-size: 22pt"}
[**Solution 1**]{color="white" bg-color="gray"} Using list comprehensions
```{pyodide-python}
scores_MSTC = [96, 92, 89, 83, 72, 66, 59, 51, 42, 38]
scores_HGE = [88, 85, 84, 80, 83, 68, 52, 63, 30, 28]
scores_PSAD = [90, 49, 83, 75, 86, 47, 56, 62, 96, 58]

rating = [0.35*mstc + 0.30*hge + 0.35*psad for mstc, hge, psad in \
          zip(scores_MSTC, scores_HGE, scores_PSAD)]
is_passed = [rating >= 70.0 and mstc >= 50.0 and hge >= 50.0 and \
             psad >= 50.0 for rating, mstc, hge, psad in \
             zip(rating, scores_MSTC, scores_HGE, scores_PSAD)]

print(rating)
print(is_passed)
```
:::

## The `numpy` library

::: {style="font-size: 22pt"}
[**Example**]{color="white" bg-color="black"}

Given the results from ten students as shown below, determine whether each student has passed the exam.
:::

::: {.fragment .fade-in style="font-size: 22pt"}
[**Solution 2**]{color="white" bg-color="gray"} Using `pandas` library's `.apply()` method
```{pyodide-python}
scores_MSTC = [96, 92, 89, 83, 72, 66, 59, 51, 42, 38]
scores_HGE = [88, 85, 84, 80, 83, 68, 52, 63, 30, 28]
scores_PSAD = [90, 49, 83, 75, 86, 47, 56, 62, 96, 58]

import pandas as pd

data = {
    "MSTC" : scores_MSTC,
    "HGE"  : scores_HGE,
    "PSAD" : scores_PSAD
}
data = pd.DataFrame(data)

def rating(data) :
    return 0.35*data["MSTC"] + 0.30*data["HGE"] + 0.35*data["PSAD"]

def is_passed(data) :
    return data["rating"] >= 70.0 and data["MSTC"] >= 50.0 and data["HGE"] >= 50.0 \
           and data["PSAD"] >= 50.0

data["rating"] = data.apply(rating, axis=1)
data["passed?"] = data.apply(is_passed, axis=1)

print(data)
```
:::

## The `numpy` library

::: {style="font-size: 22pt"}
[**Example**]{color="white" bg-color="black"}

Given the results from ten students as shown below, determine whether each student has passed the exam.
:::

::: {.fragment .fade-in style="font-size: 22pt"}
[**Solution 3**]{color="white" bg-color="gray"} Using `numpy` library
```{pyodide-python}
scores_MSTC = [96, 92, 89, 83, 72, 66, 59, 51, 42, 38]
scores_HGE = [88, 85, 84, 80, 83, 68, 52, 63, 30, 28]
scores_PSAD = [90, 49, 83, 75, 86, 47, 56, 62, 96, 58]

import numpy as np

scores_MSTC = np.array(scores_MSTC, dtype='float')
scores_HGE = np.array(scores_HGE, dtype='float')
scores_PSAD = np.array(scores_PSAD, dtype='float')

rating = 0.35*scores_MSTC + 0.30*scores_HGE + 0.35*scores_PSAD
is_passed = (rating >= 70.0) & (scores_MSTC >= 50.0) & (scores_HGE >= 50.0) \
            & (scores_PSAD >= 50.0)

print(rating)
print(is_passed)
```
:::

## The `numpy` library

::: {.fragment .fade-in style="font-size: 22pt"}
**Advantages of using `numpy` arrays over standard lists**
:::
::: {.fragment .fade-in style="font-size: 22pt"}
- Matrix and vector operations
:::
::: {.fragment .fade-in style="font-size: 22pt"}
```python
import numpy as np
A = np.array([[1, 3, 6], [-2, 5, -4], [3, -8, -7]], dtype='float')
A_inv = np.linalg.inv(A)
print(A_inv)
```
:::
::: {.fragment .fade-in style="font-size: 22pt"}
- Math operations can be applied directly
```python
rating = 0.35*scores_MSTC + 0.30*scores_HGE + 0.35*scores_PSAD
```
:::
::: {.fragment .fade-in style="font-size: 22pt"}
- Leads to shorter and more readable code
```python
rating = 0.35*scores_MSTC + 0.30*scores_HGE + 0.35*scores_PSAD
```
```python
rating = [0.35*mstc + 0.30*hge + 0.35*psad for mstc, hge, psad in \
          zip(scores_MSTC, scores_HGE, scores_PSAD)]
```
:::
::: {.fragment .fade-in style="font-size: 22pt"}
- Faster for very large array sizes [^1]
```
Concatenation:
Time taken by Lists : 0.02946329116821289 seconds
Time taken by NumPy Arrays : 0.011709213256835938 seconds

Dot Product:
Time taken by Lists : 0.179551362991333 seconds
Time taken by NumPy Arrays : 0.004144191741943359 seconds

Scalar Addition:
Time taken by Lists : 0.09385180473327637 seconds
Time taken by NumPy Arrays : 0.005884408950805664 seconds

Deletion:
Time taken by Lists : 0.01268625259399414 seconds
Time taken by NumPy Arrays : 3.814697265625e-06 seconds
```
[^1]: [https://www.geeksforgeeks.org/python/why-numpy-is-faster-in-python/](https://www.geeksforgeeks.org/python/why-numpy-is-faster-in-python/)
:::

## The `pandas` library

::: {style="font-size: 22pt"}
::: {.incremental}
::: {.fragment}
![](https://image.dienthoaivui.com.vn/x,webp,q90/https://dashboard.dienthoaivui.com.vn/uploads/dashboard/editor_upload/excel-not-responding-2.jpg)
:::

::: {.fragment}
```python
import pandas as pd

# Load the huge Excel file.
df = pd.read_csv("huge_transactions.csv")

# Get the total sales.
total_sales = df['Amount'].sum()
print(f"Total Sales: ₱{total_sales:,.2f}")
```
:::
:::
:::

## The `pandas` library

::: {style="font-size: 22pt"}
[**Example**]{color="white" bg-color="black"} Simple fruit sales data
:::

::: {.fragment .fade-in style="font-size: 22pt"}
[**Solution 3**]{color="white" bg-color="gray"} Using `numpy` library
```{pyodide-python}
import pandas as pd
import numpy as np
import matplotlib.pyplot as plt

# Import the data.
data = {
    'Date': ['2023-01-01', '2023-01-01', '2023-01-02', '2023-01-02', '2023-01-03', '2023-01-03'],
    'Product': ['Apple', 'Banana', 'Apple', 'Banana', 'Orange', 'Apple'],
    'Category': ['Fruit', 'Fruit', 'Fruit', 'Fruit', 'Fruit', 'Fruit'],
    'Units_Sold': [10, 15, np.nan, 20, 12, 5],
    'Price_Per_Unit': [1.50, 0.50, 1.50, 0.50, 0.80, 1.50]
}
df = pd.DataFrame(data)
print(df)

# Fill the missing value with the average units sold.
# WARNING: This is for illustration purposes only. Do not try this at home.
mean_units = df['Units_Sold'].mean()
df['Units_Sold'] = df['Units_Sold'].fillna(mean_units)

# Ensure 'Date' is actually a datetime object, not just a string.
df['Date'] = pd.to_datetime(df['Date'])

print(df)

# Calculate the total revenue using units sold and price per unit.
df['Total_Revenue'] = df['Units_Sold'] * df['Price_Per_Unit']

# - Group by product.
# - Get the total revenue per product.
# - Add those revenues per product.
# - Sort revenues in decreasing order.
revenue_by_product = df.groupby('Product')['Total_Revenue'].sum().sort_values(ascending=False)
print(revenue_by_product)

# Show revenue as a bar chart.
plt.figure(figsize=(8, 5))
revenue_by_product.plot(kind='bar', color='skyblue')
plt.title('Total Revenue by Product')
plt.xlabel('Product')
plt.ylabel('Revenue ($)')
plt.xticks(rotation=0)
plt.grid(axis='y', linestyle='--', alpha=0.7)
plt.show()
```
:::

## `matplotlib` and `seaborn`

::: {.fragment .fade-in style="font-size: 22pt"}
A raw list of data of number of study hours vs. grades.
```
[
  {'study_hours': 2, 'grade': 75},
  {'study_hours': 5, 'grade': 88},
  {'study_hours': 1, 'grade': 60},
  {'study_hours': 6, 'grade': 92},
  ... (Imagine 500 more rows)
]
```
:::

::: {.fragment .fade-in style="font-size: 22pt"}
Analysis using `matplotlib` and `seaborn`.
```{pyodide-python}
import seaborn as sns
import matplotlib.pyplot as plt

# Create a regression plot using seaborn.
sns.regplot(x="study_hours", y="grade", data=df)
plt.title("Do Study Hours Affect Grades?")
plt.show()
```
:::

# 5 <br/>Workshop examples

## Example 1: Rebound hammer tests

::: {layout-ncol=1 style="margin: auto;"}
<img src="https://blog.certifiedmtp.com/wp-content/uploads/2024/06/e6af71d8-6a1d-467b-98f6-e4630b1e528f.jpg" height="300"/>
:::

::: {.fragment .fade-in style="font-size: 22pt"}
[**Code**]{color="white" bg-color="gray"}
```{pyodide-python}
import numpy as np
import pandas as pd
import matplotlib.pyplot as plt

# insert code here
```
:::

## Example 2: Construction cost calculator

::: {layout-ncol=1 style="margin: auto;"}
<img src="https://freelineeng.ae/wp-content/uploads/2022/06/asian-two-business-man-construction-engineers-supervising-progress-construction-project-construction-site.jpg" height="300"/>
:::

::: {.fragment .fade-in style="font-size: 22pt"}
[**Code**]{color="white" bg-color="gray"}
```{pyodide-python}
import numpy as np
import pandas as pd
import matplotlib.pyplot as plt

# insert code here
```
:::

<!-- SLIDE: Thank you! -->

# Thank you! <br/>Any questions?

::: {.fragment style="font-size: 22pt"}

![](images/calling-card.png)

:::
